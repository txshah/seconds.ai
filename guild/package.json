{
  "name": "seconds-ai-guild-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Guild.ai trigger agent that fires the seconds.ai ingestion pipeline on a schedule",
  "dependencies": {
    "@guildai/agents-sdk": "*",
    "zod": "^3.23.0"
  }
}
